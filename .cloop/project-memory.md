

## 2026-04-10

**What was built:** Converted CHANGELOG.rst to CHANGELOG.md, migrating documentation from reStructuredText to Markdown format.

**Key patterns/conventions:**
- RST underline-based headings (`====`, `----`) converted to Markdown `#` and `##` prefix syntax
- Maintained version history structure with bullet points intact

**Gotchas:**
- File was renamed (git detected 88% similarity), not just content edited

**Files affected:**
- `CHANGELOG.rst` → `CHANGELOG.md` (renamed and converted)